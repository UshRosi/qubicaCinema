<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    Identity's tests. No container: the token service is pure, the handlers are exercised with a substitute
    UserManager, and the round-trip test hosts a tiny in-memory app on the real bearer validation, the real
    policies and the real token service, which is the guarantee this chapter is about. The register and login
    round trip against SQL Server belongs to the integration tier in chapter 7.
    Web SDK because the in-memory host needs the ASP.NET Core shared framework, as in the gateway's tests.
  -->

  <ItemGroup>
    <ProjectReference Include="../../src/Services/Identity/Identity.Api/Identity.Api.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.TestHost" />
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="xunit.v3" />
  </ItemGroup>

</Project>
