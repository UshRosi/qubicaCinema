{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      // The migration statements are worth seeing; the seed's INSERTs, with a parameter per column, are not.
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  }
}
