namespace QubicaCinema.Gateway;

/// <summary>Where the proxy configuration lives: the prefix of every key the AppHost overrides.</summary>
internal static class ReverseProxyConfig
{
    internal const string SectionName = "ReverseProxy";
}
