{
  // Used when the service or the EF tooling runs outside the AppHost: `dotnet run` on this project, or
  // `dotnet ef … --startup-project src/Services/Identity/Identity.Api`. Under the AppHost the environment
  // variables override every value here. The password and the signing key are the fixed local development
  // values declared in src/AppHost/appsettings.json; the validator refuses the key in any other environment.
  "ConnectionStrings": {
    "identitydb": "Server=localhost,1433;Database=identitydb;User Id=sa;Password=Cinema!Local1;TrustServerCertificate=True"
  },
  "Jwt": {
    "Issuer": "https://qubicacinema.local/identity",
    "Audience": "qubicacinema-api",
    "SigningKey": "qubica-cinema-local-development-signing-key-32b+"
  }
}
